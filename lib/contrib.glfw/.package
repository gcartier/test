(package contrib.glfw
  
  (root "src")
  (products (glfw (alias contrib.glfw))
            (contrib.glfw (unit glfw.product)
                          (update glfw
                                  glfw.product)
                          (dependencies jazz
                                        jazz.foreign)))
  (project ".project"))
