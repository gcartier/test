(module world.external.nbt jazz
  (import (world.external))
  (definition public TAG_End <fx>)
  (definition public TAG_Byte <fx>)
  (definition public TAG_Short <fx>)
  (definition public TAG_Int <fx>)
  (definition public TAG_Long <fx>)
  (definition public TAG_Float <fx>)
  (definition public TAG_Double <fx>)
  (definition public TAG_Byte_Array <fx>)
  (definition public TAG_String <fx>)
  (definition public TAG_List <fx>)
  (definition public TAG_Compound <fx>)
  (definition public TAG_Int_Array <fx>)
  (definition public (make-tag name type value))
  (definition public (tag-name tag))
  (definition public (tag-type tag))
  (definition public (tag-value tag))
  (definition public (tag-assoc name alist))
  (definition public (make-list-payload type <fx> content <list>) <pair>)
  (definition public (list-payload-type payload <pair>) <fx>)
  (definition public (list-payload-content payload <pair>) <list>)
  (class NBT-Reader extends Binary-Stream
    (method protected final (read-tag)))
  (class NBT-Writer extends Binary-Writer
    (method protected final (write-tag tag))))
